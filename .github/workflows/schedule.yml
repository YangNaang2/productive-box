name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets 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 }}
          GIST_ID: ${{ secrets https://gist.github.com/YangNaang2 / }}
          TIMEZONE: Asia/Seoul
